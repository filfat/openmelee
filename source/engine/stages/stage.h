#pragma once
#include <stdio.h>
#include <iostream>
#include <fstream>

namespace Engine {
    namespace Stages {
        class Stage {
            public:
                Stage(const char* path);
        };
    }
}